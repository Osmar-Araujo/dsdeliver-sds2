export type Product = {
    id: number;
    name: string;
    price: number;
    description: string;
    imagesUri: string;
}

export type OrderLocationdata = {
    latitude: number;
    longitude: number;
    address: string;
}